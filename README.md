# hackatonSeguros

Crear entorno virtual sobre repositorio clonado
  - python -m venv venv
  - pip install -r requirements.txt
  - source venv/bin/activate

Correr proyecto sobre directorio clonado
  - python -m uvicorn app.main:app --reload
